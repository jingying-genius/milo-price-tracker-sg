[phases.setup]
nixPkgs = ['python311', 'playwright-driver']

[phases.install]
cmds = [
    'pip install -r requirements.txt',
    'playwright install-deps',
    'playwright install chromium'
]

[start]
cmd = 'python complete_backend.py'
